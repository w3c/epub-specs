<note xmlns="http://docbook.org/ns/docbook" conformance="informative">    
    <para>Validation using this schema will require a processor
        that supports <xref linkend="refNVDL"/>, <xref linkend="refRelaxNG"/> and <xref
            linkend="refISOSchematron"/>.</para>
    <para>Note, however, that the NVDL schema layer can be substituted by a two-pass
        validation using the embedded RELAX NG and ISO Schematron schemas alone.</para>
</note>
