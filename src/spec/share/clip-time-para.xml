<para xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">Clock values are a subset of 
    <link xlink:href="http://www.w3.org/TR/SMIL/smil-timing.html#q31">SMIL Wallclock-sync values</link>, 
    defined in <xref linkend="refSMIL"/>. The Media Overlays schema (see <xref linkend="app-overlays-schema"/>)
    defines the syntax as <literal>hh:mm:ss.s</literal>,
    or as a single unit of hours (<literal>h</literal>),
    minutes (<literal>m</literal>), and
    seconds (<literal>s</literal>, where the optional decimal value indicates milliseconds).
    <remark role="todo">TODO: the preceding def used different tokens from the syntax. is this reformulation correct?</remark>
    See <xref linkend="app-wallclock-examples"/>. </para>

