<para xmlns="http://docbook.org/ns/docbook"> The <code>prefix</code>
    attribute must not redeclare a prefix that has already been defined through the metadata profile. Similarly, the XML
    Namespaces <xref linkend="refXMLNamespaces"/> method of defining namespace prefixes using the
        <code>xmlns</code> pseudo-attribute must not be used to define vocabulary
    prefixes. </para>
