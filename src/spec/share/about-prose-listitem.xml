<listitem xmlns="http://docbook.org/ns/docbook">
    <para>Identifies the subject of the property being expressed. The value of the 
        attribute must be an IDREF <xref linkend="refXML"/> pointing to the <literal>meta</literal>
        element containing the property it describes.
    </para>
    <para>The <literal>about</literal> attribute must be present when the <literal>meta</literal> 
        element to which it is attached is empty. When the <literal>meta</literal> element 
        contains text content, the <literal>about</literal> attribute is optional depending
        on the type of metadata being expressed.</para>
</listitem>
