
   #TODO version of encryption to use (1.0, 1.1)?
   #TODO likely not use the any-containing schema
   #TODO IDness checking

   default namespace = "urn:oasis:names:tc:opendocument:xmlns:container"
   
   include "http://www.w3.org/TR/xmlsec-rngschema/any-containing-encryption.rnc" {
      start = ocf.encryption
   }
                 
   ocf.encryption = element encryption {
     ( xenc_EncryptedData | xenc_EncryptedKey )+
   }
   
   
