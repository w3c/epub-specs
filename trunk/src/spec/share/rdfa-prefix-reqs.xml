<para xmlns="http://docbook.org/ns/docbook"> The <code role="markup">prefix</code>
	attribute <literal role="rfc2119">must not</literal> redeclare a prefix that has already been defined through the metadata profile. Similarly, the XML
    Namespaces <xref linkend="refXMLNamespaces"/> method of defining namespace prefixes using the
	<code role="markup">xmlns</code> pseudo-attribute <literal role="rfc2119">must not</literal> be used to define vocabulary
    prefixes. </para>
