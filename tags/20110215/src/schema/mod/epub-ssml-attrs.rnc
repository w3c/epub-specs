

  namespace ssml = "http://www.w3.org/2001/10/synthesis"
  
  
  html5.global.attrs &= epub.ssml.ph.attr?
  html5.html.attlist &= epub.ssml.alphabet.attr?
  
  #TODO determine injection in SVG
  #TODO SCH remains    
   
            
  epub.ssml.ph.attr = attribute ssml:ph { datatype.ssml.PhoneticExpression }
  epub.ssml.alphabet.attr = attribute ssml:alphabet { 'ipa' | 'x-SAMPA' }